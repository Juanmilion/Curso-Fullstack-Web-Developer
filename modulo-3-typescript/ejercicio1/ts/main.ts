// Definición de variables con tipos explícitos
let nombre: string = "Juanma";
let edad: any = "veinticinco"; // Esto generará un error de tipo
let activo: boolean = true;

// Imprimir las variables en la consola
console.log(nombre);
console.log(edad);
console.log(activo);
